Description:
    Generate test stubs for all public methods of Models.  
    Saves a _test_stub.rb file to avoid overwriting a real test case.

Example:
    ./script/generate model_test user

    This will create:
        test/unit/user_test_stub.rb